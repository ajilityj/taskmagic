<!DOCTYPE html>
<html lang="en">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TaskMagic</title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <link href='http://fonts.googleapis.com/css?family=PT+Sans' rel='stylesheet' type='text/css'>
  <link rel="stylesheet" href="http://code.jquery.com/ui/1.10.0/themes/base/jquery-ui.css" />
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="navbar">
    <div class="navbar-inner">
      <a class="brand" href="<%= tasks_url %>">TaskMagic</a>
      <ul class="nav">
      <% if admin_user %>
        <li><%= link_to "Platforms", platforms_url %></li>
        <li><%= link_to "Users", users_url %></li>
      <% end %>
      <% if current_user %>
        <li><%= link_to "Tasks", tasks_url %></li>
        <li><%= link_to "Calendar", calendar_tasks_url %></li>
      </ul>
      <ul class="nav pull-right">
        <li>
          <a href="users/<%= current_user.id %>/edit">
            <span class="icon-user"></span>
            <%= current_user.name %>
          </a>
        </li>
        <li style="padding-top: 10px;">|</li>
        <li><%= link_to "Logout", logout_url %></li>
      </ul>
      <% end %>
    </div>
  </div>

  <div class="container">
    <%= notice %>
    <%= yield %>
  </div>

</body>
</html>

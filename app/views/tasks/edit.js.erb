// $(".task_details #content").after("<%= j(render 'form_ajax', item: @task) %>");

<%= render "form_ajax" %>

// $('#idea_<%= @idea.id %>').replaceWith("<%= escape_javascript(render('ajax_form', idea: @idea)) %>");
